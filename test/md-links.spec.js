const mdLinks = require('../index.js');


describe('mdLinks', () => {

  it('should...', () => {
    console.log('FIX ME!');
  });
  it('Debe devolver una promesa', () => {
    expect(mdLinks()).toBeInstanceOf(Promise);
  });

});
